import { useState } from 'react'; import { Menu, X } from 'lucide-react';
export function Navigation(){const [open,setOpen]=useState(false);return <header className="nav-shell"><a className="brand" href="#top" aria-label="CSR home"><span className="brand-mark">C<span>S</span>R</span><span className="brand-sub">Collective Strategic Resources</span></a><nav className="desktop-nav" aria-label="Primary"><a href="#about">About</a><a href="#services">Services</a><a href="#safety">Safety</a></nav><a className="nav-cta" href="#contact">Contact</a><button className="menu-button" aria-expanded={open} aria-label="Toggle menu" onClick={()=>setOpen(v=>!v)}>{open?<X/>:<Menu/>}</button>{open&&<div className="mobile-nav"><a onClick={()=>setOpen(false)} href="#about">About</a><a onClick={()=>setOpen(false)} href="#services">Services</a><a onClick={()=>setOpen(false)} href="#safety">Safety</a><a onClick={()=>setOpen(false)} href="#contact">Contact</a></div>}</header>}
