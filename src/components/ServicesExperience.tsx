import {motion} from 'motion/react';import {company,type Service} from '../data/company';
const variants={hidden:{opacity:1},show:{opacity:1,transition:{staggerChildren:.11}}};
const card={hidden:{clipPath:'inset(0 100% 0 0)',x:-16},show:{clipPath:'inset(0 0% 0 0)',x:0,transition:{duration:.72,ease:[.22,1,.36,1]}}};
function ServiceCard({service,index}:{service:Service,index:number}){return <motion.a variants={card} className={`service-card card-${index+1}`} href="#contact"><img src={service.image} alt="" loading="lazy"/><div className="service-overlay"/><div className="service-orange"/><div className="service-copy"><h3>{service.name}</h3><p>{service.description}</p><span className="service-arrow" aria-hidden="true">→</span></div></motion.a>}
export function ServicesExperience(){return <section id="services" className="services"><motion.div className="services-intro" initial={{opacity:0,y:28}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.3}} transition={{duration:.7}}><div><p className="eyebrow"><i/> SERVICES</p><h2>ESSENTIAL<br/><span>INFRASTRUCTURE</span><br/>FOR EVERYDAY LIFE.</h2></div><div className="services-copy"><p>{company.servicesIntro}</p><a className="button orange-button" href="#contact">OUR SERVICES <span>→</span></a></div></motion.div><motion.div className="service-collage" variants={variants} initial="hidden" whileInView="show" viewport={{once:true,amount:.12}}>{company.services.map((s,i)=><ServiceCard key={s.id} service={s} index={i}/>)}</motion.div></section>}
